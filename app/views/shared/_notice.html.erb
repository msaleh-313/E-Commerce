<% if flash[:notice].present? %>
    <div class="fixed top-0 left-0 right-0 z-50 flex items-center justify-center p-4 pointer-events-none">
      <div class="bg-gray-100 rounded-lg border border-gray-300 shadow-md px-4 py-2 max-w-sm w-full text-center opacity-100 transform transition-transform duration-300 ease-in-out mx-auto" data-controller="notice">
        <%= flash[:notice] %>
      </div>
    </div>
  <% end %>